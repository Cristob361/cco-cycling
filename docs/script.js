document.addEventListener('DOMContentLoaded', () => {
    // All the old code related to chatToggleButton, chatbotModal,
    // and closeChatButton has been removed as it's no longer needed
    // with the Chatbase embed script.

    // You can add any future custom JavaScript for the page here.
    console.log("CCO Cycling script loaded.");
});